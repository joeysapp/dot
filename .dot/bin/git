if [[ "$1" == "status" && "$@" != *"--help"* && "$@" != *"-h"* ]];
then
    shift 1; echo_bar - 100;
    command git branches "$@"
    echo_bar - 100;
    command git status "$@"
else
    command git "$@"
fi

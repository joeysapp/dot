int main(int argc, char** argv) {

  for (int i = 1; i < argc; i++) {
    char* arg = *(argv + i);
    int address = &(*arg);
    int num = parseInt(arg);
    printf("[%i] : \"%s\" \t %i \t %i \n", i, arg, address, num);
  }
  return 0;
}
